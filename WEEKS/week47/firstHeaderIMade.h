#ifndef FIRSTHEADERIMADE_H
#define FIRSTHEADERIMADE_H

#include <stdint.h>
#include <stdbool.h>

/**
 * @brief Takes a pointer of a sring, increments a variable until it reaches the terminating char('\0').
 *
 * @param _name
 * @return int
*/

// int checkLen(char *_name);
int fakeFunc(char *_name);

bool cmpr2str(char *str1, char *str2);

#endif