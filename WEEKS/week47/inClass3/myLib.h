#ifndef MYLIB_H
#define MYLIB_H

#include <stdbool.h>

/**
 * @brief Sort an array in ascending order.
 */
    void sortArr(int *arr, int len);

/**
 * @brief Checks if the arr is sorted or not. 
*/

bool checkArrIsSorted(int *arr, int len);

#endif