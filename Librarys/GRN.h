#ifndef GRN_H
#define GRN_H

int getRandomNumber(int min, int max);

int* getArrayOfRandomNumbers(int min, int max, int count);

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "GRN.c"
#endif // !GRN_H